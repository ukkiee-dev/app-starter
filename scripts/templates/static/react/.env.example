# dev (`vite dev`) / build (`vite build`) 에서 사용하는 환경변수.
# 이 파일을 .env.local 로 복사해서 값 채움 (.env.local 은 .gitignore).
#
# Vite 규약: VITE_ prefix 가 붙은 변수만 클라이언트 번들에 인라인된다.
# 빌드 타임 인라인 방식이므로 값을 바꾸려면 이미지 재빌드 필요.

VITE_PORT=3000
